{
  "name": "rename",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "generate-icons": "node scripts/generate-icons-simple.js",
    "update-icons": "npm run generate-icons && npm run build && npm run tauri -- build --no-bundle",
    "dev-with-icons": "npm run generate-icons && npm run tauri dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.3.2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "png2icons": "^2.0.1",
    "sharp": "^0.34.3",
    "vite": "^6.0.3"
  }
}
